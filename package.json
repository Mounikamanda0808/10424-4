{
  "name": "10424-4",
  "version": "1.0.0",
  "description": ">>npm init\r >>node server.js\r >>http://localhost:8081/\r >>\"test\": \"echo \\\"Error: no test specified\\\" && exit 1\" after removing it we can run it npm start\r >>if any name is given instead of start ,we can run it by npm run name \r >>nodemon (from npm) it auto restarts the program if there is any change no need of restarting everytime\r >> npm i nodemon\r >>npm run dev\r >>inorder to ignore files .gitignore we use\r >>http status codes(browser)>>mozilla\r >> to run this in our system open terminal npm i command has to be run",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "mounika": "node server.js",
    "dev":"nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mounikamanda0808/10424-4.git"
  },
  "author": "mounika",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Mounikamanda0808/10424-4/issues"
  },
  "homepage": "https://github.com/Mounikamanda0808/10424-4#readme",
  "dependencies": {
    "nodemon": "^3.1.4"
  }
}
